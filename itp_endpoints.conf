# Auto-generated; do not edit by hand
# Endpoints migrated to Express (parity-verified) route to port 8000
location ~ ^/(?:getAllBots|getAllGasBalance|getAllYields|getAssociatedGasWallets|getContract|getEstimatedAnualYield|getGasBalance|getGasWalletPools|getSymbol|getTicks|getTotalYield)/?$ {
    limit_req zone=api burst=20 nodelay;
    if ($arg_apiKey = "vault42") { return 444; }
    if ($query_string ~* "(%60|`|api_tokens|encrypted_pk|union%20|select%20|where%20|%27|--|%2d%2d|/\*|%2f%2a)") { return 444; }

    # CORS headers
    add_header 'Access-Control-Allow-Origin' '*' always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, DELETE, PUT, OPTIONS' always;
    add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Accept, X-Requested-With' always;

    # Handle preflight OPTIONS request
    if ($request_method = 'OPTIONS') {
        add_header 'Access-Control-Allow-Origin' '*';
        add_header 'Access-Control-Allow-Methods' 'GET, POST, DELETE, PUT, OPTIONS';
        add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Accept, X-Requested-With';
        add_header 'Access-Control-Max-Age' 1728000;
        add_header 'Content-Type' 'text/plain; charset=utf-8';
        add_header 'Content-Length' 0;
        return 204;
    }

    proxy_pass http://localhost:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
# Remaining endpoints still route to R API Gateway (port 8003)
location ~ ^/(?:aaveV3|addLiquidity|approve|associateGasWallet|borrow|compoundV3|createGasWallet|deactivateCEXBot|deassociateGasWallet|deleteBot|deleteCEXBot|deleteCEXSubaccount|fluid|getAllCEXSubaccounts|getCEXSide|getCandles|getHealthFactor|getNewApiKey|getPoolAaveData|lend|linkGasWallet|llmIntrospect|mintManagerFee|poolComposition|registerCEXSubaccount|removeLiquidity|repay|setBot|setCEXSide|setCEXStrategy|unlend|unlinkGasWallet|vaultTrade)/?$ {
    limit_req zone=api burst=20 nodelay;
    if ($arg_apiKey = "vault42") { return 444; }
    if ($query_string ~* "(%60|`|api_tokens|encrypted_pk|union%20|select%20|where%20|%27|--|%2d%2d|/\*|%2f%2a)") { return 444; }

    # CORS headers
    add_header 'Access-Control-Allow-Origin' '*' always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, DELETE, PUT, OPTIONS' always;
    add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Accept, X-Requested-With' always;

    # Handle preflight OPTIONS request
    if ($request_method = 'OPTIONS') {
        add_header 'Access-Control-Allow-Origin' '*';
        add_header 'Access-Control-Allow-Methods' 'GET, POST, DELETE, PUT, OPTIONS';
        add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Accept, X-Requested-With';
        add_header 'Access-Control-Max-Age' 1728000;
        add_header 'Content-Type' 'text/plain; charset=utf-8';
        add_header 'Content-Length' 0;
        return 204;
    }

    proxy_pass http://localhost:8003;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
